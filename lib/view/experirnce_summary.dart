import 'package:flutter/material.dart';

class ExperienceSummary extends StatelessWidget {
  const ExperienceSummary({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
